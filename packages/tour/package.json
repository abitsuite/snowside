{
  "name": "@snowside/tour",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-tour.mjs",
    "serve": "node scripts/serve.mjs",
    "deploy": "pnpm run build && pnpm exec wrangler pages deploy dist --project-name snowside-tour --branch master --commit-dirty=true"
  }
}